{
  "name": "diluxone-offload-dev",
  "private": true,
  "description": "Local development environment scripts for the DiluxOne Offload WordPress plugin. Not published to npm — only used to pin @wordpress/env for contributors.",
  "license": "GPL-2.0-or-later",
  "repository": {
    "type": "git",
    "url": "https://github.com/soydiloreto/diluxone-offload-wordpress.git"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:destroy": "wp-env destroy",
    "env:reset": "wp-env destroy && wp-env start",
    "env:cli": "wp-env run cli",
    "env:logs": "wp-env logs"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@wordpress/env": "^10.0.0"
  }
}
